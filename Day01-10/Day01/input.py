# input("What is your name?")
# input() is a function that takes a string as an argument and displays it to the user
# input() also takes a string as an argument and displays it to the user as a prompt
# input() takes the user's input and returns it as a string
print("Hello " + input("what is your name?") + "!")
